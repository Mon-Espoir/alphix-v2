; Limites d'upload pour PHP-FPM / production (lu par CGI/FastCGI uniquement).
; NOTE : le serveur integre `php artisan serve` IGNORE ce fichier.
; En dev, lancer via backend/serve.sh (PHP_INI_SCAN_DIR, 50 Mo).
; Aligne sur UploadService::$maxFileSize (50 Mo).
upload_max_filesize = 50M
post_max_size = 55M
memory_limit = 256M